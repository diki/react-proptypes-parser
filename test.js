import React from 'react';
import * as salim from 'asdjkalsd';
import { haci } from 'tt';
import { four as five } from 'three';
import { six, seven } from 'eight';
import { one, two as three } from 'five';
import one, {two} from 'four';

export class test extends Component {
  static propTypes =                {
    adana 
      :    React.PropTypes.number.ashdjasd,
    hebele: React.PropTypes.number,
    karaka: React.PropTypes.oneOf({})
  }

  render() {
    return (
      <div></div>
    );
  }
}

var rcc = function() {

}

rcc.propTypes = {
  samet: React.PropTypes.number
}
